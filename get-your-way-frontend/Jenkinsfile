pipeline {
    agent any
    environment {
        CI = true
    }
        stages {
            stage('WhereamI') {
                steps {
                    sh 'pwd'
                    sh 'ls'
                }
            }
            stage('Install dependencies') {
                dir("get-your-way-frontend") {
                    steps {
                        sh 'npm install'
                        sh 'npm run build'
                        sh 'npm start'
                    }
                }
            }
        }
}